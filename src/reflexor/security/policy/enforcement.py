"""Decision enforcement helpers (placeholder).

This module will eventually contain helpers to enforce decisions at runtime (e.g., raise a
domain-level PolicyDenied error, record audit events, or open approval requests).

Clean Architecture:
The policy layer must not import infrastructure/framework layers.
"""

from __future__ import annotations
